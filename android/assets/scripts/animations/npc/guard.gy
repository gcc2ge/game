regions=game.createTextureRegions("img/npc/guard.png",25,24);
def animations=[:]
//idle 
animations<<["guard_idle":createAnimation(regions[0][0..1],0.1f)]
animations

def createAnimation(r,d){
	def anim=game.createAnimation(r,d)
	anim
}